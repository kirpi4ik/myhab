<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base window theme used before Compose draws. Compose owns the in-app theming. -->
    <style name="Theme.MyHabVoice" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:statusBarColor">@color/primary</item>
    </style>
</resources>
